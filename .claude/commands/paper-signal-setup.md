# PaperSignal Setup

Set up PaperSignal for a non-technical user, through conversation. **Read and follow
`claude-code/skills/paper-signal-setup/SKILL.md` exactly** — do not act from this stub.

Non-negotiables: you run every command and solve every error yourself (the user never
sees YAML, commands, or tracebacks); get consent before installing; tune the first report
in preview mode (`--no-mark-seen`) per the skill; persist their mode choice in
`daily.report_mode`.
