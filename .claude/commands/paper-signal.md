# PaperSignal

Generate today's paper report. **Read and follow
`claude-code/skills/paper-signal/SKILL.md` exactly** — do not act from this stub.

Non-negotiables: honor `daily.report_mode` from the config (explicit user request
overrides); commit after writing; use plain language with the user (no CLI jargon or raw
output); never overwrite a note the quick scan didn't write.
