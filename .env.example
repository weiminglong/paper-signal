OBSIDIAN_VAULT_PATH="/path/to/your/Obsidian Vault"

# Optional overrides — the defaults work for a normal clone, so leave these commented.
# Use ABSOLUTE paths (schedulers run with a different working directory).
# PAPER_SIGNAL_CONFIG="/absolute/path/to/config/interests.yaml"
# PAPER_SIGNAL_BIN="/absolute/path/to/paper-signal"
